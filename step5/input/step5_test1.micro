PROGRAM step4
BEGIN

	INT a, b, i, p;

	FUNCTION VOID main()
	BEGIN

		i := 0;
		a := 0;
		b := 0;

		WHILE(i != 10)
			READ (p);
			
			IF (p > 10) 
				a := a + 1;
			ELSE
      			b := b + 1;
			ENDIF
			i := i+1;
		ENDWHILE

		WRITE (a);
		WRITE (b);
	END
END